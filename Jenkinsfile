pipeline {
    agent 
    {
      label 'jenkins_job'
    }

    stages {
        stage('Build') {
            steps {
                sleep(time: 5, unit: 'SECONDS')
            }
        }

        stage('Deploy') {
            steps {
                sleep(time: 5, unit: 'SECONDS')
            }
        }

        stage('Test') {
            steps {
                sleep(time: 5, unit: 'SECONDS')
            }
        }
    }
}
